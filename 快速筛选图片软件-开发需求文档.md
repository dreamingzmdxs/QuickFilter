# 图片快速筛选软件 — 开发需求文档

- 版本：v1.0（需求确认版）
- 日期：2026-02
- 状态：已与需求方确认，可进入设计/开发阶段

---

## 1. 项目概述

开发一套"图片快速筛选"工具，用于从大量图片中按个人判断快速完成分类、删除与跳过操作。系统由两部分组成：

| 部分 | 载体 | 技术选型（已确认） |
|------|------|------------------|
| 桌面端 | Windows 桌面程序 | C# + WinUI 3（.NET 8） |
| 移动端 | 安卓配套 App | Kotlin 原生 App |
| 通信 | 局域网（同一 Wi-Fi / 同一网段） | WebSocket（指令） + HTTP（图片传输）+ UDP（自动发现） |

**核心使用场景**：用户指定一个源目录和两个目标目录，程序自动读取源目录中的图片并逐张展示（每次一张）。用户通过**鼠标拖拽手势**（左滑 / 右滑 / 上滑 / 下滑）或**键盘按键**（方向键 / WASD）完成分类操作；同时可使用安卓手机在局域网内**同屏查看并遥控**（手机上滑动与电脑端等效）。

**规模指标**：单次筛选可达 **上万张** 图片，程序必须保证流畅、不卡死。

---

## 2. 用户操作定义（已确认）

| 操作 | 鼠标手势 | 键盘（方向键） | 键盘（WASD） | 效果 |
|------|---------|--------------|-------------|------|
| 分入左目标目录 | 点住图片后**左滑** | ← | A | 将当前图片移动至左目标目录 |
| 分入右目标目录 | 点住图片后**右滑** | → | D | 将当前图片移动至右目标目录 |
| 删除 | 点住图片后**上滑** | ↑ | W | 将当前图片移动至"已删除"目录 |
| 跳过 | 点住图片后**下滑** | ↓ | S | 当前图片不处理，直接进入下一张 |
| 撤销上一步 | — | Ctrl+Z | Ctrl+Z | 将上一步已移动的图片移回原位置 |

- 每次操作执行后自动进入下一张图片。
- 滑动判定：鼠标按下后位移超过阈值（建议 ≥ 40 px）且在某个方向上占优（该方向位移 > 另一方向位移的 1.5 倍）时触发对应操作；触发后给出视觉反馈（方向箭头/色块提示）。

---

## 3. 全局流程

```
用户启动程序
  → 配置会话：源目录 + 左目标目录 + 右目标目录 (+ 可选"已删除"目录)
  → 扫描源目录，生成待处理队列（按配置的排序规则）
  → 逐张显示图片
  → 用户执行操作（桌面手势/键盘/安卓端遥控）
  → 程序执行文件操作 + 记录日志 + 更新进度
  → 队列清空或用户退出
```

**续传能力**：会话状态持久化到本地文件（详见 §6.3）。程序异常退出或手动关闭后，再次打开同一源目录时提示"继续上次会话 / 新建会话"，选择继续则从未处理的第一张接续。

---

## 4. 桌面端（Windows）功能需求

### 4.1 会话配置界面
- 提供目录选择控件：源目录（必填）、左目标目录（必填）、右目标目录（必填）、已删除目录（选填）。
- 已删除目录未填写时，默认使用 `源目录\已删除`（自动创建）。
- 目标目录/已删除目录不存在时自动创建。
- 校验：源目录必须存在；目标目录不得是源目录的父目录/子目录（防递归移动产生歧义）；左右目标不得相同。
- 配置项（保存在设置中，可记忆上次使用路径）：
  - 排序规则：文件名升序（默认）/ 修改时间 / 随机。
  - 是否递归子目录（默认开）。
  - 快速预览模式开关（大图降采样显示，用于追求速度的场景）。

### 4.2 图片扫描
- 格式支持：`.jpg` `.jpeg` `.png` `.webp` `.bmp` `.gif` `.heic`（经系统 HEIF 扩展解码，未安装扩展时提示用户在微软商店安装）。
- **递归子目录并保留结构**：源目录下子文件夹内的图片也纳入筛选；移动时保留相对路径结构（如 `源目录\a\b.jpg` → `左目标\a\b.jpg`）。
- 排序规则确认后生成队列：文件名序 / 修改时间序 / 随机（洗牌后固定，同一会话内顺序不再变化，保证续传一致）。
- 扫描为异步操作，显示进度（"已发现 N 张"），扫描期间 UI 不冻结。

### 4.3 图片展示（主界面）
- 单张全屏区域居中展示，等比缩放适配窗口（"适应窗口"模式）。
- **每次只显示一张**；顶部或底部信息栏显示：文件名、格式、像素尺寸、文件大小、修改时间（或 EXIF 拍摄时间，如有）、当前进度（第 N / 共 M 张）。
- 方向提示条：左右方向显示目标目录名，上方显示"删除"，下方显示"跳过"，并实时显示对应目录的累计数量。
- 快捷键提示（底部小字）。

### 4.4 缩放与平移（已确认需要）
- 鼠标滚轮缩放：建议范围 0.1x – 8.0x，以光标为中心缩放。
- **缩放 = 1.0（适应窗口）时**：鼠标点住拖拽 = 滑动分类手势（见 §2）。
- **缩放 > 1.0 或 < 1.0 时**：鼠标点住拖拽 = 平移视图；此时滑动分类通过键盘或安卓端执行（界面需提示当前处于"缩放模式，拖拽为平移"）。
- 双击图片：复位到"适应窗口"并随机回到分类手势可用状态；也可用 Esc 复位。
- 缩放模式下仍可点击图片边缘或使用键盘操作。

### 4.5 文件操作规则（已确认）
- **移动**（非复制）：图片从源目录移入目标目录；"删除"同样为移动到已删除目录（**不做物理删除，保证可恢复**）。
- **同名冲突**：目标目录已存在同名文件时，自动重命名加序号：`photo.jpg` → `photo_1.jpg` → `photo_2.jpg`……原文件不丢失、不覆盖。
- 移动失败（文件被占用/权限/磁盘错误）时：停留在当前图片，弹出错误提示并记录到日志，不进入下一张。
- 文件操作在后台异步执行；执行成功后再切换下一张（避免用户看到下一张而文件尚未完成）。
- 撤销（Ctrl+Z）：将最近一次已移动的图片移动回源目录原位置（含相对路径）；可连续撤销（会话内撤销栈，上限建议 100 步）；撤销不改变"跳过"记录。安卓端提供撤销按钮，与 Ctrl+Z 等效。

### 4.6 断电/崩溃续传（已确认需要）
- 会话状态文件见 §6.3；每次文件移动成功后立即更新状态文件（写盘，保证崩溃后最多丢失当前这 1 张的状态判定——该文件仍留在源目录，下一轮继续处理，不重复移动）。
- 打开程序 → 选择源目录 → 若检测到状态文件则弹窗："检测到未完成的会话（已处理 123/500，剩余 377），是否继续？"
  - 继续：恢复队列顺序与跳过记录，从第一张未处理处展示。
  - 新建：忽略旧状态文件开始新会话（旧文件备份为 `.bak`）。

### 4.7 操作日志与 CSV 导出（已确认需要）
- 程序内实时记录每次操作：时间、文件名、相对路径、操作类型（左/右/删除/跳过/撤销）、源路径、目标路径。
- CSV 导出按钮（会话内任意时刻可导出；退出时如果未导出则提示）。
- CSV 列：`时间, 文件名, 相对路径, 操作, 源路径, 目标路径, 结果`。

### 4.8 进度与完成
- 界面顶部进度条 + 数字（第 N / 共 M），左右目录与删除目录计数实时更新。
- 队列空时显示完成页：统计（左 N、右 N、删除 N、跳过 N），并提供"导出 CSV""重新开始"按钮。

### 4.9 性能要求（上万张规模）
- **懒加载**：仅解码当前图片（快速预览模式下解码为窗口尺寸，≤ 1920px；正常模式全尺寸解码）；前后各预解码 2 张备用，内存中最多保留 5 张图，出窗口即释放。
- 解码与文件 IO 全部在后台线程/Task 中执行，UI 线程零阻塞。
- 打开 1 万张图（主要为 jpg）时：扫描 ≤ 2s，切换图片展示 ≤ 150ms（快速预览模式）；内存峰值 ≤ 800MB。
- GIF 动图仅显示第一帧（或提供开关播放动图，默认关）。

---

## 5. 安卓端功能需求（已确认：同屏显示 + 遥控）

### 5.1 连接
- **自动发现**：App 启动后在局域网内广播/扫描（UDP 组播或 mDNS），列出发现的"快速筛选"服务；也可**手动输入电脑 IP + 端口**连接。
- 连接建立：桌面端提示"设备 <名称> 请求连接"，需在电脑端确认（防止误连）；支持首次配对后记住设备（免确认）。
- **单台设备**：同一时刻仅一台设备可连接；新设备连接请求会让桌面端提示"替换当前设备？"。
- 断线（设备息屏/网络抖动）：App 显示断线并自动重连（重试间隔建议 3s，最多 10 次后需手动）；电脑端显示"设备已断开"，桌面端流程不受影响（可继续用键盘/鼠标）。

### 5.2 同屏显示
- 手机实时显示电脑当前图片（压缩图，建议服务端按手机屏幕宽度输出 WebP/JPEG，≤ 1280px 宽）。
- 显示信息栏：文件名、进度（第 N / 共 M）、左右/删除/跳过计数、连接状态。
- 图片可达性：图片切换延迟（手机可见新图）目标 ≤ 300ms。
- 手机端也支持缩放（双指），缩放后拖拽平移；与桌面端规则一致的提示。

### 5.3 遥控操作
- 手机滑动：左右滑 = 左右目录、上滑 = 删除、下滑 = 跳过（与桌面端一致的判定阈值）。
- 提供等效按钮列（← → ↑ ↓ / 撤销），便于单手握持时点按。
- 手机上的操作指令发送到电脑端，**由电脑端执行**（电脑端为权威）：执行成功后广播下一张图片与最新状态；执行失败（如文件冲突异常）时手机端收到错误并提示。
- 电脑端收到遥控指令后同样记录到操作日志。

### 5.4 手机端界面
- 首屏：设备发现列表 / 手动连接输入框、最近使用记录。
- 操作界面：图片区（占主体）+ 底部操作按钮 + 顶部进度/信息栏。
- 连接成功时顶部状态灯变绿；断线变红并提示。

---

## 6. 通信协议设计（草案）

### 6.1 通道
- **发现**：UDP 广播（端口建议 47800），桌面端周期性广播 `FILTER-DISCOVER {name, version, ip, port}`；App 监听并响应。
- **指令通道**：WebSocket（`ws://<pc-ip>:<port>`，端口建议 47900），JSON 文本消息。
- **图片通道**：HTTP `GET /image/{sessionId}/{index}?width=1280`，返回压缩图（带缓存头）。图片不通过 WebSocket 传输，简化断点与压缩控制。

### 6.2 消息定义（JSON，示例）
```
C→S: {"type":"auth","pin":"4852","device":"Pixel8","appVersion":"1.0.0"}
S→C: {"type":"auth-ok","sessionId":"..."}
S→C: {"type":"state","index":123,"total":500,"filename":"a.jpg","counts":{"left":40,"right":50,"del":10,"skip":23},"imageUrl":"/image/.../123?width=1280"}
C→S: {"type":"gesture","action":"left"}            // left|right|delete|skip
S→C: {"type":"ack","action":"left","ok":true}
S→C: {"type":"error","code":"MOVE_FAILED","message":"..."}
C→S: {"type":"undo"}
```
- 全部消息带单调递增序号，用于幂等重试（离线时重发）。
- 心跳：每 5s 一次，20s 无响应判定断线。

### 6.3 会话状态文件
- 位置：`源目录\.quickfilter-session.json`（隐藏文件）。
- 内容：`{version, sessionId, startTime, orderRule, queue:[...递归扫描的相对路径+排序], processed:{path:action}, skipList:[...], undoStack:[...], deletedDir, leftDir, rightDir}`。
- 每次操作成功后增量写盘；新建会话时若文件存在则先备份副本。

---

## 7. 非功能需求

| 类别 | 要求 |
|------|------|
| 平台 | Windows 10 2004+ / Windows 11；安卓 8.0+ |
| 性能 | 见 §4.9 与 §5.2 指标 |
| 可靠性 | 任意时刻崩溃最多影响当前 1 张；状态文件保证续传；移动失败不丢图 |
| 安全 | 局域网内 PIN 配对；HTTPS 非必需（局域网），但传输建议做轻量加密（可选：WebSocket 摘要鉴权）；不做公网暴露 |
| 可维护性 | 核心逻辑（队列/冲突重命名/撤销/会话持久化）与 UI 解耦，核心逻辑提供单元测试 |
| 打包 | 桌面端 WinUI 3 自包含打包（x64）；安卓端 APK（及可选 AAB） |

---

## 8. 项目里程碑（建议）

| 阶段 | 内容 | 交付物 |
|------|------|--------|
| M1 | 桌面端 MVP：会话配置、扫描、单张展示、鼠标手势 + 键盘操作、移动/删除（含冲突重命名）、撤销、进度显示 | 可用的 Windows 单机版 |
| M2 | 桌面端完善：递归子目录保留结构、排序配置、缩放/平移、EXIF/文件名信息、CSV 导出、HEIC 支持、上万张性能优化、会话续传 | 完整的 Windows 版 |
| M3 | 安卓端：设备发现、PIN 配对、同屏显示、遥控、断线重连 | 可用的安卓版 |
| M4 | 局域网联调、压测（1 万张 + 手机遥控）、打包发布 | 安装包（MSIX/EXE + APK） |

---

## 9. 验收标准（摘要）

1. 用 1 万张 jpg（含子目录结构）测试：扫描完成 ≤ 2s，切图 ≤ 150ms，无界面卡死。
2. 左右目标目录分别收到对应图片；上滑图片进入已删除目录；下滑图片保留在源目录并被记录为已跳过。
3. 同名文件冲突自动加序号，无覆盖、无丢失。
4. 移动中途杀掉进程，重新打开可续传且不重不漏（图片不会重复移动、不会漏处理）。
5. Ctrl+Z 连续撤销 5 步，图片准确回到源目录原路径。
6. 手机上滑动/按钮与电脑操作等效；断网后电脑端可继续独立工作；重连后状态一致。
7. HEIC 图片可正常显示（系统已装 HEIF 扩展时）。
8. 操作日志 CSV 可导出，内容与界面显示一致。

---

## 10. 待确认/开放问题

- 目标目录为空时是否创建；左右目录是否允许直接使用绝对路径（已默认允许）。
- GIF 动图默认只显示首帧，是否需要"播放动画"开关（**已实现**：配置页勾选，默认关）。
- 会话状态文件是否随源目录移动/拷贝（已默认随源目录存放）。
- 是否需要"未处理图片"的临时离开功能（如临时暂停并切到别的目录，回来继续）——当前默认不需要。
- 安卓端是否要求屏幕常亮（建议默认开启，设置中可关）。

> 进度：M1 桌面端 MVP 与 M2 桌面端完善（缩放/平移、EXIF、GIF 开关、快速预览、预加载、上万张性能验证）已完成；
> 待开发：M3 安卓端同屏遥控、M4 联调与打包发布。详见 README 里程碑进度。
